Blockly.Python['scraped1'] = function(block) {
  var value_src = Blockly.Python.valueToCode(block, 'src', Blockly.Python.ORDER_ATOMIC);
  // TODO: Assemble Python into code variable.
  var code = 'scraped1 = ' + value_src + '\n';
  return code;
};

Blockly.Python['scraped2'] = function(block) {
  var value_src = Blockly.Python.valueToCode(block, 'src', Blockly.Python.ORDER_ATOMIC);
  // TODO: Assemble Python into code variable.
  var code = 'scraped2 = ' + value_src + '\n';
  return code;
};

Blockly.Python['scraped3'] = function(block) {
  var value_src = Blockly.Python.valueToCode(block, 'src', Blockly.Python.ORDER_ATOMIC);
  // TODO: Assemble Python into code variable.
  var code = 'scraped3 = ' + value_src + '\n';
  return code;
};

Blockly.Python['scraped4'] = function(block) {
  var value_src = Blockly.Python.valueToCode(block, 'src', Blockly.Python.ORDER_ATOMIC);
  // TODO: Assemble Python into code variable.
  var code = 'scraped4 = ' + value_src + '\n';
  return code;
};

Blockly.Python['scraped5'] = function(block) {
  var value_src = Blockly.Python.valueToCode(block, 'src', Blockly.Python.ORDER_ATOMIC);
  // TODO: Assemble Python into code variable.
  var code = 'scraped5 = ' + value_src + '\n';
  return code;
};

Blockly.Python['get_scraped1'] = function(block) {
  var value_arg = Blockly.Python.valueToCode(block, 'arg', Blockly.Python.ORDER_ATOMIC);
  // TODO: Assemble Python into code variable.
  var code = 'scraped1'+ value_arg;
  // TODO: Change ORDER_NONE to the correct strength.
  return [code, Blockly.Python.ORDER_NONE];
};

Blockly.Python['get_scraped2'] = function(block) {
  var value_arg = Blockly.Python.valueToCode(block, 'arg', Blockly.Python.ORDER_ATOMIC);
  // TODO: Assemble Python into code variable.
  var code = 'scraped2'+ value_arg;
  // TODO: Change ORDER_NONE to the correct strength.
  return [code, Blockly.Python.ORDER_NONE];
};

Blockly.Python['get_scraped3'] = function(block) {
  var value_arg = Blockly.Python.valueToCode(block, 'arg', Blockly.Python.ORDER_ATOMIC);
  // TODO: Assemble Python into code variable.
  var code = 'scraped3'+ value_arg;
  // TODO: Change ORDER_NONE to the correct strength.
  return [code, Blockly.Python.ORDER_NONE];
};

Blockly.Python['get_scraped4'] = function(block) {
  var value_arg = Blockly.Python.valueToCode(block, 'arg', Blockly.Python.ORDER_ATOMIC);
  // TODO: Assemble Python into code variable.
  var code = 'scraped4'+ value_arg;
  // TODO: Change ORDER_NONE to the correct strength.
  return [code, Blockly.Python.ORDER_NONE];
};

Blockly.Python['get_scraped5'] = function(block) {
  var value_arg = Blockly.Python.valueToCode(block, 'arg', Blockly.Python.ORDER_ATOMIC);
  // TODO: Assemble Python into code variable.
  var code = 'scraped5'+ value_arg;
  // TODO: Change ORDER_NONE to the correct strength.
  return [code, Blockly.Python.ORDER_NONE];
};