Blockly.Python['array_scraped1'] = function(block) {
  var value_idx = Blockly.Python.valueToCode(block, 'idx', Blockly.Python.ORDER_ATOMIC);
  var value_arg = Blockly.Python.valueToCode(block, 'arg', Blockly.Python.ORDER_ATOMIC);
  // TODO: Assemble Python into code variable.
  var code = 'scraped1[' + value_idx + ']'+value_arg;
  // TODO: Change ORDER_NONE to the correct strength.
  return [code, Blockly.Python.ORDER_NONE];
};

Blockly.Python['array_scraped2'] = function(block) {
  var value_idx = Blockly.Python.valueToCode(block, 'idx', Blockly.Python.ORDER_ATOMIC);
  var value_arg = Blockly.Python.valueToCode(block, 'arg', Blockly.Python.ORDER_ATOMIC);
  // TODO: Assemble Python into code variable.
  var code = 'scraped2[' + value_idx + ']'+value_arg;
  // TODO: Change ORDER_NONE to the correct strength.
  return [code, Blockly.Python.ORDER_NONE];
};

Blockly.Python['array_scraped3'] = function(block) {
  var value_idx = Blockly.Python.valueToCode(block, 'idx', Blockly.Python.ORDER_ATOMIC);
  var value_arg = Blockly.Python.valueToCode(block, 'arg', Blockly.Python.ORDER_ATOMIC);
  // TODO: Assemble Python into code variable.
  var code = 'scraped3[' + value_idx + ']'+value_arg;
  // TODO: Change ORDER_NONE to the correct strength.
  return [code, Blockly.Python.ORDER_NONE];
};

Blockly.Python['array_scraped4'] = function(block) {
  var value_idx = Blockly.Python.valueToCode(block, 'idx', Blockly.Python.ORDER_ATOMIC);
  var value_arg = Blockly.Python.valueToCode(block, 'arg', Blockly.Python.ORDER_ATOMIC);
  // TODO: Assemble Python into code variable.
  var code = 'scraped4[' + value_idx + ']'+value_arg;
  // TODO: Change ORDER_NONE to the correct strength.
  return [code, Blockly.Python.ORDER_NONE];
};

Blockly.Python['array_scraped5'] = function(block) {
  var value_idx = Blockly.Python.valueToCode(block, 'idx', Blockly.Python.ORDER_ATOMIC);
  var value_arg = Blockly.Python.valueToCode(block, 'arg', Blockly.Python.ORDER_ATOMIC);
  // TODO: Assemble Python into code variable.
  var code = 'scraped5[' + value_idx + ']'+value_arg;
  // TODO: Change ORDER_NONE to the correct strength.
  return [code, Blockly.Python.ORDER_NONE];
};